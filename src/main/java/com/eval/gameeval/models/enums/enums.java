package com.eval.gameeval.models.enums;

public class enums {
    public enum UserRole{
        SUPER_ADMIN,
        ADMIN,
        SCORER,
        NORMAL
    }
    public enum ProjectStatus {
        NOT_STARTED,
        ONGOING,
        ENDED
    }
}
